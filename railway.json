{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "nixpacksConfigPath": "backend/nixpacks.toml"
  },
  "deploy": {
    "startCommand": "cd backend && python manage.py migrate --noinput && python manage.py collectstatic --noinput && gunicorn centro.wsgi --chdir backend --bind 0.0.0.0:$PORT",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
